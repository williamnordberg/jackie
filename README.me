1. Installing Redis on Your Local Machine
Steps to Install Redis Locally:
On Linux (Ubuntu/Debian):

sh

sudo apt update
sudo apt install redis-server

On macOS:
brew install redis

On Windows:

You can use the Windows Subsystem for Linux (WSL) to install Redis or use a pre-compiled Redis for Windows from Memurai or Redis Labs.
Start Redis Server:
redis-server
